namespace SeguraChain_Lib.Instance.Node.Network.Enum.Manage
{
    public enum ClassPeerEnumInsertStatus
    {
        EMPTY_PEER_IP = 0,
        INVALID_PEER_PORT = 1,
        PEER_ALREADY_EXIST = 2,
        INVALID_PEER_IP = 3,
        EXCEPTION_INSERT_PEER = 4,
        PEER_INSERT_SUCCESS = 5
    }
}
